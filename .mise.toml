[tools]
node = "22"
pnpm = "10.27.0"

[tasks.setup]
description = "Full dev setup: install tools, deps, build, link libref CLI"
run = """
mise install
pnpm install
pnpm --filter @wbisschoff13/libref build
(cd packages/libref && pnpm link --global)
"""
